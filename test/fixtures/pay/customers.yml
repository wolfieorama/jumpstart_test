fake:
  owner: fake_processor (Account)
  processor: fake_processor
  processor_id: fake_1000
  default: true

subscribed:
  owner: subscribed (Account)
  processor: fake_processor
  processor_id: fake_1001
  default: true
